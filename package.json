{
  "name": "blockcredai",
  "version": "1.0.0",
  "description": "Decentralized Employment Verification with AI Fraud Detection",
  "private": true,
  "scripts": {
    "test": "npm --prefix backend test && npm --prefix blockchain test && npm --prefix frontend run typecheck",
    "build": "npm --prefix backend run build && npm --prefix frontend run build && npm --prefix blockchain run compile",
    "test:e2e": "npx playwright test",
    "test:e2e:headed": "npx playwright test --headed",
    "test:e2e:ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "playwright": "^1.63.0"
  }
}
